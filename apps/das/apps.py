from django.apps import AppConfig


class DasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.das"
    label = "das"
    verbose_name = "Guias fiscais (DAS)"
